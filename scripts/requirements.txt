# Python dependencies for GitHub Releases upload script
PyGithub>=2.1.1,<3.0.0

# Python dependencies for version extraction
PyYAML>=6.0.0,<7.0.0

# Note: iOS scripts (ios_*.py) use only Python standard library
# No additional dependencies required for iOS operations
